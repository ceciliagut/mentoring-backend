#!/bin/sh

echo "Running ktlint..."
./gradlew ktlintFormat --daemon -q

if [ $? -ne 0 ]; then
  echo "ktlint failed. Please review the output above."
  exit 1
fi

# Re-stage any files ktlint may have auto-formatted
git add -u
