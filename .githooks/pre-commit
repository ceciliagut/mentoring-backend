#!/bin/sh

echo "Running kotlinter..."
./gradlew formatKotlin --daemon -q

if [ $? -ne 0 ]; then
  echo "formatKotlin failed. Please review the output above."
  exit 1
fi

# Re-stage any files kotlinter may have auto-formatted
git add -u
